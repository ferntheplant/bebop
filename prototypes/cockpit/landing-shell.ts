process.stdout.write(`stage       validating\n`);
process.stdout.write(`controller  swordfish\n`);
process.stdout.write(`cowboy      none\n`);
process.stdout.write(`bebop       connected\n\n`);
process.stdout.write(`PROTOTYPE: an ordinary shell prompt follows the one-shot status.\n`);

setInterval(() => undefined, 60_000);
process.stdin.resume();
